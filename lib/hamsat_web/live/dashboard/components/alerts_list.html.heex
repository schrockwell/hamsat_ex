<div class="panel">
    <%= if @alerts == [] do %>
        <%= render_slot @empty %>
    <% end %>
    <%= for alert <- @alerts do %>
        <.live_component module={AlertItem} id={"#{@id}-#{alert.id}"} alert={alert} now={@now} context={@context} mine?={@mine?} />
    <% end %>
</div>